# encoding: utf-8
# programa que et diu si les 2 dades que has introduït es menor, igual o major que 0
primer = int(input("Introdueix el primer nombre: "))
segon = int(input("Introdueix el segon nombre: "))
if primer > 0:
    print "El primer nombre es major que 0"
if primer < 0:
	print "El primer nombre es menor que 0"
if primer == 0:
	print "El primer nombre es 0"
if segon > 0:
    print "El segon nombre es major que 0"
if segon < 0:
	print "El segon nombre es menor que 0"
if segon == 0:
	print "El segon nombre es 0"
